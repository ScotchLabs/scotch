<%= form_for(@election) do |f| %>
  <% if @election.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@election.errors.count, "error") %> prohibited this election from being saved:</h2>

      <ul>
      <% @election.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :position %><br />
    <%= f.collection_select :position, Election::POSITIONS, :to_s, :to_s, :include_blank => true %>
  </div>
  <div class="field">
    <%= f.label :platform %><br />
    <%= f.text_area :platform %>
  </div>
  <div class="actions">
    <%= f.submit "Create" %>
  </div>
<% end %>
