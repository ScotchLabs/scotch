<!DOCTYPE html>
<html lang="en">
<head>
  <title>Scotch'n'Soda Theatre</title>
  <meta charset='utf-8'>
  <%= stylesheet_link_tag "style" %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
  <div id='wrapper'>
    <div id='bannernocss3' class='banner'></div>
    <div id='banner' class='banner'></div>
    <section>
      <header>
        <h1><a href='/'>Scotch on the Web</a></h1>
        <h2>Flavortext?</h2>
      </header>
      <nav>
        <ul>
          <li class='bottomleftcorner'><%= link_to "Dashboard", dashboard_index_path, :class => 'bottomleftcorner' %></li>
          <li <%= nav_class(users_path)%> ><%= link_to "Users", users_path %></li>
          <li <%= nav_class(groups_path)%> ><%= link_to "Groups", groups_path %></li>
          <li <%= nav_class(items_path)%> ><%= link_to "Inventory", items_path %></li>
        </ul>
      </nav>
      <% if @group && ! @group.new_record? %>
        <nav>
          <ul>
            <li><%= @group %></li>
            <li <%= nav_class(group_events_path(@group)) %>><%= link_to "Events", group_events_path(@group) %></li>
            <li <%= nav_class(group_positions_path(@group)) %>><%= link_to "Members", group_positions_path(@group) %></li>
            <li <%= nav_class(group_checkouts_path(@group)) %>><%= link_to "Checkouts", group_checkouts_path(@group) %></li>
            <!-- <li <%= nav_class(group_documents_path(@group)) %>><%= link_to "Documents", group_documents_path(@group) %></li> -->
          </ul>
        </nav>
      <% end %>
      <noscript>
        <article>
          <p>Hey there! It looks like you've got JavaScript turned off.
            Unfortunately, the Powers of the Internet don't work so well without it.
            We've tried to accommodate people in your boat, but the truth is there
            are some landlocked lakes, if you follow the way I've been drifting.
            No guarantees.</p>
        </article>
      </noscript>
      <% if !notice.nil? or !alert.nil? %>
        <article>
          <p class="notice"><%= notice %></p>
          <p class="alert"><%= alert %></p>
        </article>
      <% end %>
      <% if yield.include?("<article") %>
        <%= yield %>
      <% else %>
        <article><%= yield %><div class='clearfix'></div></article>
      <% end %>
      <div id='push'></div>
    </section>
    <% if request.domain == "localhost" %>
    <aside>
      <%= debug params %>
    </aside>
    <% end %>
  </div> 
    <footer>
      <b>Scotch on the Web</b> is designed and developed by the Scotch'n'Soda Web Team<br />
      &copy; 2008-2010 Scotch'n'Soda Theatre at <b>Carnegie mellon University</b><br />
      Current Version <%= render "layouts/version" %> | Help build Scotch on the Web!
    </footer>
    <div id='sodabar'>
      how to do <a href='#'>soda</a>?
    </div>
</body>
</html>
