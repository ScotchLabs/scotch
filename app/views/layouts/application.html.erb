<!DOCTYPE html>
<html lang="en">
<head>
  <title>Scotch'n'Soda Theatre</title>
  <meta charset='utf-8'>
  <%= stylesheet_link_tag 'style', 'colorbox', 'jquery.autocomplete', :cache => true %>
  <%= javascript_include_tag "http://ajax.googleapis.com/ajax/libs/jquery/1.4.1/jquery.min.js" %>
  <%= javascript_include_tag :defaults, :cache => true %>
  <%= csrf_meta_tag %>
  <% if current_page? dashboard_calendar_path %>
    <%= stylesheet_link_tag "fullcalendar.css" %>
    <%= javascript_include_tag "fullcalendar.min.js" %>
  <% end %>
</head>
<body>
  <div id='wrapper'>
    <a name='top'></a>
    <section>
      <header id='header'>
        <h1><a href='/'>Scotch on the Web</a></h1>
        <h2><%= flavortext %></h2>
      </header>
      <nav id='nav'>
        <% if current_user then %>
          <%= render "layouts/nav" %>
        <% else %>
          <%= render "layouts/login" %>
        <% end %>
      </nav>
      <noscript>
        <article>
          <p>Hey there! It looks like you've got JavaScript turned off.
            Unfortunately, the Powers of the Internet don't work so well without it.
            We've tried to accommodate people in your boat, but the truth is there
            are some landlocked lakes, if you follow the way I've been drifting.
            No guarantees.</p>
        </article>
      </noscript>
      <% if current_user and (!notice.nil? or !alert.nil?) %>
        <article>
          <p class="notice"><%= notice %></p>
          <p class="alert"><%= alert %></p>
        </article>
      <% end %>
      <%= yield %>
      <div id='push'></div>
    </section>
  </div>
  <% if current_user %>
    <ul id='logout'>
      <% if request.domain == "localhost" %>
      <li><a href='#' id='debuglink'>Show Debug</a></li>
      <div class='hidden'>
        <div id='debugblock'>
          <h2>Params</h2>
          <%= debug params %>
        </div>
      </div>
      <script>$(document).ready(function(){$("#debuglink").colorbox({inline:true,href:"#debugblock",opacity:0.25,transition:'none',width:'50%'})})</script>
      <% end %> 
      <li><%= link_to "Feedback", new_feedback_path %></li>
      <li><%= link_to "Log Out", destroy_user_session_path %></li>
    </ul>
  <% end %>
  <footer id='footer'>
    <b>Scotch on the Web</b> is designed and developed by the <%= link_to "Scotch'n'Soda Web Team", Group.system_group %><br>
    &copy; 2008-2010 <%= link_to "Scotch'n'Soda Theatre", "http://snstheatre.org" %> at <b>Carnegie Mellon University</b><br>
    Current Version <%= render "layouts/version" %> | <%= mail_to current_webmaster.email, "Help build Scotch on the Web!" %><br>
    <%= link_to "Terms of Use & Privacy Policy", dashboard_terms_url %> | <%= link_to "Back to Top", '#top' %>
  </footer>
  <%= render :partial => 'layouts/sodabar' %>
</body>
</html>
