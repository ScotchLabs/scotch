<!DOCTYPE html>
<html lang="en">
<head>
  <title>Scotch'n'Soda Theatre</title>
  <meta charset='utf-8'>
  <%= javascript_include_tag "http://ajax.googleapis.com/ajax/libs/jquery/1.4.1/jquery.min.js" %>
  <%= stylesheet_link_tag 'style', 'colorbox' %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
  <div id='wrapper'>
    <div id='bannernocss3' class='banner'></div>
    <div id='banner' class='banner'></div>
    <% if current_user %>
      <ul id='logout'>
        <li><%= link_to "Feedback", new_feedback_path %></li>
        <li><%= link_to "Log Out", destroy_user_session_path %></li>
      </ul>
    <% end %>
    <section>
      <header>
        <h1><a href='/'>Scotch on the Web</a></h1>
        <h2><%= flavortext %></h2>
      </header>
      <nav>
        <% if current_user then %>
          <%= render "layouts/nav" %>
        <% else %>
          <%= render "layouts/login" %>
        <% end %>
      </nav>
      <noscript>
        <article>
          <p>Hey there! It looks like you've got JavaScript turned off.
            Unfortunately, the Powers of the Internet don't work so well without it.
            We've tried to accommodate people in your boat, but the truth is there
            are some landlocked lakes, if you follow the way I've been drifting.
            No guarantees.</p>
        </article>
      </noscript>
      <% if !notice.nil? or !alert.nil? %>
        <article>
          <p class="notice"><%= notice %></p>
          <p class="alert"><%= alert %></p>
        </article>
      <% end %>
      <% if yield.include?("<article") %>
        <%= yield %>
      <% else %>
        <article><%= yield %><div class='clearfix'></div></article>
      <% end %>
      <div id='push'></div>
    </section>
    <% if request.domain == "localhost" %>
    <aside>
      <a href='#' id='debuglink' style='color:#fff'>Show Debug</a>
      <div class='hidden'>
        <div id='debugblock'>
          <h2>Params</h2>
          <%= debug params %>
        </div>
      </div>
      <script>$(document).ready(function(){$("#debuglink").colorbox({inline:true,href:"#debugblock",opacity:0.25,transition:'none',width:'50%'})})</script>
    </aside>
    <% end %>
  </div> 
    <footer>
      <b>Scotch on the Web</b> is designed and developed by the Scotch'n'Soda Web Team<br>
      &copy; 2008-2010 <%= link_to "Scotch'n'Soda Theatre", "http://snstheatre.org" %> at <b>Carnegie Mellon University</b><br>
      Current Version <%= render "layouts/version" %> | <%= mail_to current_webmaster.email, "Help build Scotch on the Web!" %>
    </footer>
    <div id='sodabar'>
      how to do <a href='#'>soda</a>?
    </div>
</body>
</html>
