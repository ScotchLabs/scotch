<article>
<% if @user == current_user %>
  <h1>My Followed Things</h1>
<% else %>
  <h1><%= @user %>'s Followed Things</h1>
<% end %>

  <% @watchers.compact.each do |watcher| %>
    <%= as_line_item(watcher.time) %>
  <% end %>
</article>
