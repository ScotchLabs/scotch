<%= simple_form_for [@list, @message].compact do |f| %>
  <% if @list %>
    <%= hidden_field_tag :message_list_id, value: @list.id %>
  <% end %>
  <%= f.input :subject %>
  <%= f.input :text %>
  <%= f.button :submit %>
<% end %>
