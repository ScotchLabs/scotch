<article>
<h1>Editing Group</h1>

<%= render 'form' %>
</article>

<% if @group and not @group.archived? then %>
    <% if @group.class.name == "Board" then %>
      <%= render 'archive_board' %>
    <% else %>
      <%= render 'archive_group' %>
    <% end %> 
<% end %>
