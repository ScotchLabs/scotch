<%= form_for(@knomination) do |f| %>
  <% if @knomination.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@knomination.errors.count, "error") %> prohibited this knomination from being saved:</h2>

      <ul>
      <% @knomination.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
