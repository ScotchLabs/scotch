<h1>Listing events</h1>

<% @events.each do |event| %>
  <div>
    <h3><%= link_to event, event %></h3>
    <%= event.start %> to <%= event.end %> at <%= event.location %>
  </div>
<% end %>

<br />

<%= link_to 'New Event', new_group_event_path(@group) %>
