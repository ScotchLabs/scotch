<article>
<h1>New checkout for <% if @group %>Group "<%= @group.name %>"<% else %>Item "<%= @item.name %>"<% end %></h1>

<%= render 'form' %>

<br>
<% if @group %><%= link_to "Back to #{@group}", group_checkouts_path(@group) %>
<% elsif @item %><%= link_to "Back to #{@item}", item_checkouts_path(@item) %><% end %>
</article>
